sudo: required

language: python
python:
  - 3.6

services:
  - docker

install:
  - pip install mkdocs
  - docker --version
  - docker build -t quantumleap .

script:
  - sh run_tests.sh

after_success:
    - sh ./deploy_docs.sh

notifications:
  email: false
